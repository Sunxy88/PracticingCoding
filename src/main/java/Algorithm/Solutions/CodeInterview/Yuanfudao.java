package Algorithm.Solutions.CodeInterview;

import java.util.*;
public class Yuanfudao {

    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        String boxStr = in.nextLine();
        char[] boxes = boxStr.toCharArray();
        Deque<Character> boxStack = new LinkedList<>();
        Deque<Integer> countStack = new LinkedList<>();
    }
}
